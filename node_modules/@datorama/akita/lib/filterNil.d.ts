import { Observable } from 'rxjs';
import { Diff } from './types';
/**
 * @example
 *
 * query.selectEntity(2).pipe(filterNil)
 */
export declare const filterNil: <T>(source: Observable<T>) => Observable<Diff<T, null>>;
